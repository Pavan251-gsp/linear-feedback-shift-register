`timescale 1ns / 1ps
module tb(

    );
    reg clk,rst;
    wire [3:0]a;
    lfsr dut(.a(a),.clk(clk),.rst(rst));
    always #5 clk=~clk;
    initial begin
    clk=0;
    rst=1;
    @(negedge clk); rst=0;
   repeat(10)@(negedge clk); 
  @(negedge clk); rst=1;
  @(negedge clk);rst=0;
  repeat(4) @(negedge clk);
  
  $finish;
  end
endmodule
